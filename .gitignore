# Claude Code session files
.claude/

# macOS
.DS_Store
.AppleDouble

# iCloud Drive sync-conflict copies (this repo lives in iCloud Drive, which
# spawns "filename 2.ext" duplicates when files are written quickly, e.g. builds)
* [0-9].*
* [0-9]
*.icloud

# Editor
.vscode/
*.swp
*~

# Local server logs
nohup.out

# Desktop build artifacts (rebuild with: cd desktop && npm install && npm run build)
desktop/node_modules/
desktop/game/
desktop/dist/
desktop/icon.iconset/

# Web build artifact (rebuild with: ./build-web.sh)
web-dist/
